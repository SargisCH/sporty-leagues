export type League = {
  idLeague: string;
  strLeague: string;
  strSport: string;
  strLeagueAlternate: string;
};

export type Country = League;

export type Season = {
  strSeason: string;
  strBadge: string;
};
